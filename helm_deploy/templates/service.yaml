apiVersion: v1
kind: Service
metadata:
  name: {{ include "laa-assess-non-standard-magistrate-fee-app.fullname" . }}
  labels:
    {{- include "laa-assess-non-standard-magistrate-fee-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "laa-assess-non-standard-magistrate-fee-app.selectorLabels" . | nindent 4 }}
