---
en:
  search:
    show_all: Show all
    statuses:
      not_assigned: Not assigned
      in_progress: In progress
      provider_updated: Provider updated
      sent_back: Sent back
      granted: Granted
      auto_grant: Auto-granted
      part_grant: Part granted
      rejected: Rejected
      expired: Expired
    unassigned: Unassigned
  nsm:
    searches:
      show:
        page_title: Search for a claim
        query:
          label: Enter any combination of client or firm name, UFN or LAA reference


  activemodel:
    errors:
      models:
        nsm/search_form:
          attributes:
            submitted_from:
              not_a_date: Enter a valid submission date from
            submitted_to:
              not_a_date: Enter a valid submission date to
            updated_from:
              not_a_date: Enter a valid last updated from
            updated_to:
              not_a_date: Enter a valid last updated to
